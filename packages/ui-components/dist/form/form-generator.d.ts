import { FormGeneratorProps } from './types';
export default function FormGenerator({ form, formItems, submitText, onSubmit, onFailed, isInline, submitButtonLoading, ...props }: FormGeneratorProps): JSX.Element;
